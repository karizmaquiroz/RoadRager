using UnityEngine;

//add to the shop booster panel
public class BuyBooster : MonoBehaviour
{
    public void BuyUpgrades()
    {
        //SaveManager.instance.money -= upgradesPrice[currentUpgrade];
        //SaveManager.instance.
    }
}
